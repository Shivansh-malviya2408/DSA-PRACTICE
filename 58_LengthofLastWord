class Solution {
    public int lengthOfLastWord(String s) {

         s=s.trim(); 
        int j=s.length()-1,count=0,n=s.length();
        while( j>=0 && s.charAt(j)!=' ')
        {
            count++;
            j--;
        }

         
        return count;




    }
}